export default class SectionFour {

    constructor(svg) {
        this.svg = svg;

        this.svgWidth = parseInt(this.svg.attr('width'));
        this.svgHeight = parseInt(this.svg.attr('height'));
    }

    onInit() {

    }

    onFocusEntered() {
    }

    onFocusLost() {
    }
}